enum UserStatus {
  ACTIVE
  INACTIVE
  BLOCKED
}

type User {
  id: Int
  name: String!
  email: String!
  age: Int
  salary: Float
  vip: Boolean
  profile: Profile
  status: UserStatus
}

type Profile {
  id: Int
  name: String!
}
